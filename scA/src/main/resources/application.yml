spring:

  #数据库连接信息
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/nybatisplus?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  #数据库初始化配置
  sql:
    init:
      mode: always
  config:
    import: classpath:bootstrap.yml


server:
  port: 8080
#eureka:
 # client:
 #   service-url:
 #     defaultZone: http://127.0.0.1:8081/eureka
